apiVersion: v1
kind: Service
metadata:
  name: processflow-frontend-service
spec:
  type: NodePort
  selector:
    app: processflow-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
