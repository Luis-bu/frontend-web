apiVersion: v1
kind: Service
metadata:
  name: processflow-backend-service
spec:
  type: NodePort
  selector:
    app: processflow-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081
